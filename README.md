# rag-document-assistant
А conversational RAG assistant for querying uploaded documents using GPT-4o, LangGraph, LangChain, and ChromaDB. Implemented document ingestion pipeline, semantic retrieval, conversational memory, and source-grounded answer generation via Streamlit interface.
